# Minimal deps for DJtransGAN inference (not the 2021 full freeze).
# Skip `acoustics` (noise synth for training only; its sdist breaks on non-ASCII Windows paths).

torch>=1.13,<2.3
torchaudio>=0.13,<2.3
numpy>=1.21,<1.26
scipy>=1.7
librosa>=0.9,<0.11
soundfile>=0.10
matplotlib>=3.5
joblib>=1.0
gdown>=4.5
tqdm>=4.60
pyloudnorm>=0.1.0
pyrubberband>=0.3.0
nnAudio>=0.3.1
torchlibrosa>=0.0.9
openunmix>=1.2.0
auraloss>=0.2.0
IPython>=7.0
# madmom: install separately with --no-build-isolation after Cython (see README)
